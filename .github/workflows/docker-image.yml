name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
env:
  IMAGE: masterid/cloud9_jupyter
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Login to DockerHub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}
    - name: Docker Build
      run: docker build . --file Dockerfile_commondb_scripts --tag $IMAGE_FILLER
    - name: Docker Push
      run: docker push $IMAGE_FILLER
